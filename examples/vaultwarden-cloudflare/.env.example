# Template only. Copy to `.env`, fill in REAL values, and never commit your .env.
# selfhosted-doctor still flags these as plaintext secrets to demonstrate the check.
TUNNEL_TOKEN=FAKE_TUNNEL_TOKEN_FOR_TESTS_ONLY
ADMIN_TOKEN=EXAMPLE_ONLY_ADMIN_TOKEN_DO_NOT_USE
