# FAKE template. Copy to .env and set real values before deploying.
# These are placeholder defaults, so the scanner reports them as low-priority
# template findings — not high-severity leaked secrets.
SECRET_KEY=sk-EXAMPLE_ONLY_DO_NOT_USE
DB_PASSWORD=difyai123456
POSTGRES_PASSWORD=difyai123456
